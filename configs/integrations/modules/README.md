# Module Integration Configs

Per-module registration files consumed by AIOps for discovery and monitoring.
Each module's `configs/module.yaml` is the source of truth; entries here are synced copies.
